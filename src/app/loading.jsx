'use client'

export default function Loading() {
  return (
    <div className="flex items-center justify-center h-screen">
      <h2 className="text-gray-900 text-4xl capitalize animate-pulse">Please wait a moment...</h2>
    </div>
  )
}
